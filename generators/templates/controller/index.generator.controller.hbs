import {
  Controller,
  Get,
  Post,
  Body,
  Patch,
  Param,
  Delete,
  Query,
} from '@nestjs/common';
import { Ok } from '../responses/success.types';
import { Create{{titleCase name}}Dto } from 'src/application/DTOs/create-{{kebabCase name}}.dto';
import { Create{{titleCase name}}Dto } from 'src/application/DTOs/update-{{kebabCase name}}.dto';
import { {{titleCase name}}Service } from 'src/application/services/{{kebabCase name}}.service';

@Controller('/{{kebabCase name}}')
export class {{titleCase name}}Controller {
  constructor( 
    private readonly _{{camelCase name}}Service: {{titleCase name}}Service
  ) {}
  
  @Get()
  public async findAll() {
    const {{camelCase name}} = this._{{camelCase name}}Service.findAll();
    return Ok({{camelCase name}});
  }
  @Get()
  public async findByPrimary(@Param('id') id: string) {
    const {{camelCase name}} = this._{{camelCase name}}Service.findByPrimary(id);
    return Ok({{camelCase name}});
  }
  @Post()
  public async add(
    @Body() create{{camelCase name}}Dto: Create{{titleCase name}}Dto,
    @Param('id') id:string) {
    const {{camelCase name}} = this._{{camelCase name}}Service.create(create{{titleCase name}}Dto, id);
    return {{camelCase name}}
  }
  @Put()
  public async update(@Body() update{{camelCase name}}Dto: Update{{titleCase name}}Dto) {
    const {{camelCase name}} = this._{{camelCase name}}Service.update(id);
    return Ok({{camelCase name}});
  }
  @Delete()
  public async delete(@Param('id') id: string) {
    const {{camelCase name}} = this._{{camelCase name}}Service.delete(
      id,
    );
    return Ok({{camelCase name}});
  }
}

