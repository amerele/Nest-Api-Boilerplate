import { IsBoolean, IsNumber, IsString } from 'class-validator';

export class Create{{titleCase name}}Dto {
  @IsNumber()
  cd{{titleCase name}}: number;

  constructor(
    cd{{titleCase name}}: number,
  ) {
    this.cd{{titleCase name}} = cd{{titleCase name}};
  }
}
