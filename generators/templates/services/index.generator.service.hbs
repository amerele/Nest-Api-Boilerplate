import { Injectable } from '@nestjs/common';
import { {{titleCase name}}Repository } from 'src/infraestructure/repositories/{{kebabCase name}}.repository';
import { Create{{titleCase name}}Dto } from '../DTOs/{{camelCase name}}/create-{{kebabCase name}}.dto';

@Injectable()
export class {{titleCase name}}Service {
  constructor(private readonly _{{camelCase name}}Repository: {{titleCase name}}Repository) {}

  public async findAll() {
    try {
      return this._{{camelCase name}}Repository.findAll();
    } catch (error) {
      console.log(error);
      throw error;
    }
  }

  public async findByPrimary(id: string) {
    try {
      return this._{{camelCase name}}Repository.findByPrimary(id);
    } catch (error) {
      console.log(error);
      throw error;
    }
  }

  public async create{{titleCase name}}(
    create{{camelCase name}}Dto: Create{{titleCase name}}Dto,
    id: string,
  ) {
    try {
      return this._{{camelCase name}}Repository.createOrUpdate{{titleCase name}}(
        create{{camelCase name}}Dto,
        id,
      );
    } catch (error) {
      console.log(error);
      throw error;
    }
  }

  public async delete{{titleCase name}}(id: string) {
    try {
      return this._{{camelCase name}}Repository.delete{{titleCase name}}(id);
    } catch (error) {
      console.log(error);
      throw error;
    }
  }
}
