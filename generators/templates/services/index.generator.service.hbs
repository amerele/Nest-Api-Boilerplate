import { Injectable } from '@nestjs/common';
import { {{titleCase name}}Repository } from 'src/infraestructure/repositories/{{kebabCase name}}.repository';
import { Body{{titleCase name}}Dto } from '../DTOs/{{camelCase name}}/body-{{kebabCase name}}.dto';

@Injectable()
export class {{titleCase name}}Service {
  constructor(private readonly _{{camelCase name}}Repository: {{titleCase name}}Repository) {}

  public async findAll() {
    try {
      return this._{{camelCase name}}Repository.findAll();
    } catch (error) {
      console.log(error);
      throw error;
    }
  }

  public async findByPrimary(id: number) {
    try {
      return this._{{camelCase name}}Repository.findByPrimary(id);
    } catch (error) {
      console.log(error);
      throw error;
    }
  }

  public async createOrUpdate(
    body: Body{{titleCase name}}Dto,
    id: number,
  ) {
    try {
      return this._{{camelCase name}}Repository.createOrUpdate(
        body,
        id,
      );
    } catch (error) {
      console.log(error);
      throw error;
    }
  }

  public async delete(id: number) {
    try {
      return this._{{camelCase name}}Repository.delete(id);
    } catch (error) {
      console.log(error);
      throw error;
    }
  }
}
